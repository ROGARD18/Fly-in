[project]
name = "Fly-in"
version = "0.1.0"
description = "Introduction to gps"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flake8>=7.3.0",
    "mypy>=1.20.2",
    "pydantic>=2.13.3",
]
